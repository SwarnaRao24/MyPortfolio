version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3  # <-- This makes your database persistent and permanent!
    environment:
      - DEBUG=False
    restart: always